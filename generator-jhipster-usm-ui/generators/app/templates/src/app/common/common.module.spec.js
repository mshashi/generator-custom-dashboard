'use strict';

describe('Common', function() {

    beforeEach(module('<%= camelAppName %>.common'));

    it('exists', function() {
        // even those there are no expects in this test,
        // it will still test that your dependencies are
        // defined correctly because angular has to inject
        // it in order to execute this 'it' block
    });

});
